> This project is for screen test purposes only

## Installation
- Install dependencies by runing `yarn` or `npm install`

## Development
- Android `npx react-native run-android`
- iOS `npx react-native run-ios` 

## Build APK
- Run `cd android && ./gradlew installRelease`